# Session N — <date> (<module>)

> **Copy this to `docs/sessions/<YYYY-MM-DD>.md` at the end of a working
> session.** Gitignored, like `NEXT.md`. The difference between the two:
> `NEXT.md` is *state* and gets overwritten; this is *history* and does not.

Continues `docs/sessions/<previous>.md`, which <one line>.

**<The headline: what this session actually achieved.>**

**N commits, gate green, pushed / deliberately not pushed because <reason>.**

## Summary

1. …
2. …

---

# <The main piece of work>

What was built, and the decisions inside it that were not obvious.

## <A decision worth keeping>

State the alternative that was rejected and why. A decision recorded without its
alternative gets re-made.

---

# Mistakes made and caught

**Do not skip this section.** It is the most valuable part of the file, and the
one that is tempting to leave out. Each entry: what was wrong, how it surfaced,
and what would have caught it sooner.

A useful distinction to record: whether a check *failed because the thing was
broken* or *because the check was wrong*. Both happen, and confusing them costs
hours.

---

# State

A table of where things stand, and what is committed but unpushed.

# Handoff

**Read**: `README.md`, `COURSE-STRUCTURE.md`, `NEXT.md`, then this file.

**Start**: the one thing to do next, and why it is that one.

## Open for <the instructor>

The things waiting on a human — a domain check, a decision, something only they
can test.
