# NEXT — <module>

> **Copy this to `NEXT.md`.** It is gitignored, along with `docs/sessions/`, so
> it never reaches a build — and it is the first thing to read when picking the
> project up again. Keep it *current* rather than complete: a stale NEXT.md is
> worse than none, because it is trusted.

Current working state. Updated frequently. Not committed (see `.gitignore`).

Last updated: <date> (<one line on what changed>)

## Current Status

Where the course is, in a sentence, then a table. What is written, what is
domain-checked, what is released. **Released is a separate thing from written** —
`pixi run status` is the only answer to what is live.

| | |
|---|---|
| L1 | released |
| L2–Ln | written, held back |
| P1–Pn | written, held back |

## Next Tasks

**START HERE: <the one thing>.** One task, not a list — the list is below it.

Then everything else, grouped by whether it is blocked:

### Blocked on someone else

Numbered, each with what exactly is needed and from whom. Strike through and
date them as they close, rather than deleting: *why* something was decided is
the part that gets lost.

### Doable any time

The small things. Poll URLs, a figure that would improve a session, a backport.

### Never do

The boundaries that are easy to cross by accident. Every course has some — an
answer key that must not be uploaded, a machine a tool must not be validated on.

## Open Questions

Things not yet decided, each with what would settle it. Convert to a decision
with a date when answered.

## Known Issues / Risks

Failure modes that have bitten, or would. **Silent ones first** — anything where
the build stays green and the mistake reaches students.

## Recent Decisions

Newest first, dated, one section per working session. This is the part that
makes a six-month gap survivable, so record the *reason*, not just the outcome.
